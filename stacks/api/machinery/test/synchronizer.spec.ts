import {Synchronizer} from "@spica-server/machinery";

describe("Synchronizer", () => {
  describe("fileToResource", () => {
    let synchronizer: Synchronizer;

    beforeEach(() => {
    });
  });
});
