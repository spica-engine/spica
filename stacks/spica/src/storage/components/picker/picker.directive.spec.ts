import {PickerDirective} from "./picker.directive";

describe("PickerDirective", () => {
  it("should create an instance", () => {
    const directive = new PickerDirective(null);
    expect(directive).toBeTruthy();
  });
});
