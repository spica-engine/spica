{
  "name": "api/status",
  "sourceRoot": "libs/api/status",
  "projectType": "library",
  "targets": {
    "build": {
      "command": "tsc --build libs/api/status/tsconfig.json --pretty --verbose"
    },
    "test": {
      "dependsOn": [
        {
          "projects": ["api/function/runtime/node/bootstrap"],
          "target": "build"
        }
      ]
    }
  }
}
