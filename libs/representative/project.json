{
  "name": "representative",
  "sourceRoot": "libs/representative/src",
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/representative",
        "main": "libs/representative/src/index.ts",
        "tsConfig": "libs/representative/tsconfig.lib.json"
      },
      "dependsOn": ["^build"]
    }
  }
}
