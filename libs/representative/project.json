{
  "name": "representative",
  "sourceRoot": "libs/representative/src",
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "libs/representative/dist",
        "main": "libs/representative/src/index.ts",
        "tsConfig": "libs/representative/tsconfig.lib.json",
        "transformers": [{"name": "typescript-transform-paths/nx-transformer"}]
      },
      "dependsOn": ["^build"]
    }
  }
}
