{
  "name": "api/function/packages/identity",
  "sourceRoot": "apps/api/src/function/packages/identity",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["transform-paths"],
      "options": {
        "command": "tsc --build {projectRoot}"
      }
    },
    "transform-paths": {
      "executor": "nx:run-commands",
      "options": {
        "command": "resolve-tspaths"
      }
    }
  }
}
