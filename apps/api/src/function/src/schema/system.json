{
  "$id": "http://spica.internal/function/enqueuer/system",
  "type": "object",
  "required": ["name"],
  "properties": {
    "name": {
      "title": "Event",
      "type": "string",
      "enum": ["READY"],
      "description": "Lifecycle event of the instance",
      "default": "READY"
    }
  },
  "additionalProperties": false
}
