{
  "name": "mongoreplicationcontroller",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/mongoreplicationcontroller/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "serve": {
      "executor": "@nx/js:node",
      "options": {
        "buildTarget": "mongoreplicationcontroller:build"
      }
    },
    "build": {
      "executor": "@nx/js:tsc",
      "inputs": [
        "{workspaceRoot}/tsconfig.json",
        "{projectRoot}/src/**/*",
        "{projectRoot}/tsconfig.json",
        "{projectRoot}/project.json"
      ],
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/mongoreplicationcontroller",
        "main": "apps/mongoreplicationcontroller/src/main.ts",
        "tsConfig": "apps/mongoreplicationcontroller/tsconfig.json"
      },
      "cache": true
    },
    "build-image": {
      "inputs": [
        "{workspaceRoot}/tsconfig.json",
        "{projectRoot}/src/**/*",
        "{projectRoot}/tsconfig.json",
        "{projectRoot}/project.json",
        "{projectRoot}/Dockerfile"
      ],
      "command": "docker buildx build --platform=${PLATFORM:-linux/amd64} --load . -f ./apps/mongoreplicationcontroller/Dockerfile -t spicaengine/mongoreplicationcontroller:$VERSION",
      "cache": true
    },
    "publish": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker buildx build --platform=${PLATFORMS:-linux/amd64,linux/arm64} --push . -f ./apps/mongoreplicationcontroller/Dockerfile -t spicaengine/mongoreplicationcontroller:$VERSION"
      }
    }
  }
}
