{
  "name": "panel",
  "sourceRoot": "apps/panel",
  "projectType": "application",
  "targets": {
    "build-image": {
      "command": "docker build --platform=linux/amd64 --target runner . -f ./apps/panel/Dockerfile -t spicaengine/panel:local-test --build-arg BASE_URL=$BASE_URL --build-arg VITE_BASE_URL=$VITE_BASE_URL"
    },
    "publish": {
      "executor": "nx:run-commands",
      "dependsOn": ["build-image"],
      "options": {
        "command": "docker push spicaengine/panel:$VERSION"
      }
    }
  }
}
