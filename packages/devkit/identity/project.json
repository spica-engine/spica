{
  "name": "devkit/identity",
  "sourceRoot": "packages/devkit/identity",
  "projectType": "library",
  "targets": {
    "bundle": {
      "dependsOn": ["build"],
      "command": "yarn rollup -c {projectRoot}/rollup.config.cjs --bundleConfigAsCjs"
    },
    "set-version": {}
  }
}
